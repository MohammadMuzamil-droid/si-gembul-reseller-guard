# GEMINI_API_KEY: Required for Gemini AI API calls in local development.
# Keep the local value outside version control. In Cloud Run, bind this environment
# variable from Google Cloud Secret Manager; do not hardcode a key in source.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# PORT is optional locally. Cloud Run supplies it at runtime.
PORT="3000"
